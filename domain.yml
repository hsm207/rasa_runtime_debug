version: "2.0"

intents:
  - intent_one
  - intent_two

entities:
  - entity_one
  - entity_two

actions:
  - action_one
  - action_two

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
